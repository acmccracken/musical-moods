<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Show</title>
</head>
<body>
    <table>
        <tr>
            <td>Song:</td>
            <td><%= song.songName %></td>
        
            <td>Artist: </td>
            <td><%= song.artistName %></td>
        
            <td>Mood:</td>
            <td><%= song.mood %></td>

            <td>Year:</td>
            <td><%= song.year %></td>
            <td>
            <form action="/songs/<%= song._id %>?_method=DELETE" method="POST">
                <button type="submit" id="deletebtn">X</button>
            </form>
        </td>
            <td>
                <form action="/songs/<%= song._id %>?_method=UPDATE" method="POST">
                <label>Change Mood:
                    <select name="mood">
                        <option value="Peaceful">Peaceful</option>
                        <option value="Hype">Hype</option>
                        <option value="Transcendental">Transcendental</option>
                        <option value="Relaxed">Relaxed</option>
                        <option value="Happy">Happy</option>
                    </select>
                    <button type="submit" id="updatebtn">CHANGE</button>

                </label>
                </form>
            </td>

        </tr>
            <td>
                <form action="/songs/<%= song._id %>?_method=UPDATE" method="POST">
                    <label>Rate Song:
                        <select name="rating.songRating">
                            <option value=1>1</option>
                            <option value=2>2</option>
                            <option value=3>3</option>
                            <option value=4>4</option>
                            <option value=5>5</option>
                        </select>
                        <button type="submit" id="updatebtn2">Rate Song</button>
            </td>
            
                <% if (song.rating == true) {%>
                    <td><%= song.mood %></td>
                }
                <%}%>
            
        <tr>

        </tr>
        </table>
    
</body>
</html>